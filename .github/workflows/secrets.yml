name: Secret Detection

on:
  push:
  pull_request:

jobs:
  quality:
    permissions:
      contents: read
    uses: $/.github/workflows/tox-check-reusable.yml
    with:
      tox_env: secret-detection